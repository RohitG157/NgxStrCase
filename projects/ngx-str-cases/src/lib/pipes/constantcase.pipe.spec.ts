import { ConstantcasePipe } from './constantcase.pipe';

describe('ConstantcasePipe', () => {
  it('create an instance', () => {
    const pipe = new ConstantcasePipe();
    expect(pipe).toBeTruthy();
  });
});
